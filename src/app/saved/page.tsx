import { SavedClient } from "@/components/SavedClient";

export const metadata = {
  title: "Saved",
  description: "Articles you set aside to read later.",
};

export default function SavedPage() {
  return <SavedClient />;
}
